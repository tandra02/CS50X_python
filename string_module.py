# In the string module, there are constants digits and ascii_lowercase. Print the value of digits constant in the first line of the 
# output, and the value of ascii_lowercase in the second line.

import string
print(string.digits)
print(string.ascii_lowercase)